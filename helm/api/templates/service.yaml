apiVersion: v1
kind: Service
metadata:
  name: {{ include "sort-api-v2.fullname" . }}
  labels:
    {{- include "sort-api-v2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "sort-api-v2.selectorLabels" . | nindent 4 }}
