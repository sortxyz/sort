/**
 * In this evaluation mode, organizations can have only a single user.
 *
 * Enter your email addresses and passwords here.
 * This is just for evaluation purposes so plaintext password is fine.
 */
export const users: Record<string, string> = {
  "demouser@example.com": "password",
};
